/** Route params for the field app's stack navigator. */
export type FieldStackParams = {
  Tabs: undefined;
  FamilyDetail: { familyId: string };
  VisitForm: { familyId: string };
  AssessmentForm: { familyId: string };
  AssistanceForm: { familyId: string };
  BeneficiaryForm: { familyId: string };
};
